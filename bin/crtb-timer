#!/usr/bin/env raku
use v6.d;
use App::Classroom::Toolbox;

use Terminal::Print <T>;

sub MAIN(UInt :$minutes = 15) {
    T.initialize-screen;
    timer(T, $minutes);
    prompt;
    T.shutdown-screen;
}