#!/usr/bin/env raku
use v6.d;
use App::Classroom::Toolbox::Timer;

sub MAIN(UInt:D :min($minutes), #= Minutes the timer shall run
         Bool   :$skip-wait,    #= Wait for return/enter key after timer finished?
         ) {
    run-timer($minutes, $skip-wait)
}